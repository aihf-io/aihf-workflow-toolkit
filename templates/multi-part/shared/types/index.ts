/**
 * Shared type definitions for this suite.
 *
 * Export your domain types here so all workflow bundles can import them:
 *   import { MyEntity } from '@suite/shared/types';
 */

export interface ExampleEntity {
  id: string;
  name: string;
  createdAt: string;
}
